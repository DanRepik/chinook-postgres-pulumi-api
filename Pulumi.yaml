name: chinook-api
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: Chinook API built using API-Foundry
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
